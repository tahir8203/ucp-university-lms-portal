{
  "name": "university-lms-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Mobile APK wrapper for the University LMS Firebase web app.",
  "type": "module",
  "scripts": {
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "apk:debug": "cd android && gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/cli": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {}
}
